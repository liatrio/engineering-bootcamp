---
docs/9-kubernetes-container-orchestration/9.6-keda.md:
  category: Container Orchestration
  estReadingMinutes: 20
  exercises:
    - name: Meet the System
      description: Deploy a Redis-backed order processing system using k3d and the provided Taskfile. Verify all services are running before proceeding.
      estMinutes: 30
      technologies:
        - Docker
        - Kubernetes
        - Redis
    - name: Simulate Peak Traffic
      description: Run a flash sale load simulation and observe the order-processor fall behind as queue depth grows unchecked.
      estMinutes: 20
      technologies:
        - Docker
        - Kubernetes
        - Redis
        - k6
    - name: Attempt a Fix with Native HPA
      description: Write and apply a CPU-based HPA targeting the order-processor, then re-run the load test and evaluate its effectiveness.
      estMinutes: 45
      technologies:
        - Docker
        - Kubernetes
    - name: Introduce KEDA
      description: Remove the native HPA and install KEDA into the cluster using Helm.
      estMinutes: 30
      technologies:
        - Kubernetes
        - KEDA
    - name: Create a ScaledObject
      description: Read the KEDA documentation and write a ScaledObject that scales the order-processor based on Redis queue depth.
      estMinutes: 60
      technologies:
        - Kubernetes
        - KEDA
        - Redis
    - name: Run the Load Test Again
      description: Re-run the flash sale simulation and compare queue depth and scaling behavior against the previous two runs.
      estMinutes: 20
      technologies:
        - Kubernetes
        - KEDA
        - Redis
        - k6
---

# 9.6 KEDA

KEDA (Kubernetes Event-Driven Autoscaler) is an open-source project that extends Kubernetes with the ability to scale workloads based on signals from external event sources — message queues, databases, HTTP request rates, and more. Where a native HPA reacts to resource metrics like CPU and memory, KEDA can react to the thing that actually drives resource consumption: the work waiting to be done.

This distinction matters most for bursty, queue-driven workloads. CPU is a lagging indicator: by the time a worker pod's CPU spikes, it has already been processing a backlog for some time. Queue depth is a leading indicator — it reflects exactly how much work is waiting right now. Scaling on queue depth lets a system respond before pressure builds rather than after.

This section builds on the HPA concepts from 9.5 using a companion e-commerce example in `examples/ch9/keda/`. You will deploy a realistic producer/consumer system, observe it fail under load, attempt a fix with a native HPA, and then replace it with KEDA's event-driven autoscaling.

## Exercise 1 — Meet the System

Going forward with this section, we will be working with a faux e-commerce order system. It can be found in the `examples/ch9/keda` directory of the [bootcamp repository](https://github.com/liatrio/engineering-bootcamp).

The example system has two services:

- **checkout-service** — an HTTP service that accepts orders from customers and pushes them onto a Redis queue
- **order-processor** — a worker that pops orders from the queue and processes them one at a time

This producer/consumer pattern is one of the most common architectures in distributed systems. It decouples the rate at which work arrives from the rate at which it is processed, allowing each service to be scaled and deployed independently.

Take some time to explore the project and understand it's structure. You don't need to understand every line, but knowing how the different components of the system interact will be critical to completing this exercise. In particular, look at the [Taskfile](https://taskfile.dev/docs/guide) and get a feel for how it can help orchestrate the cluster.

Once you have a grasp on how the system works, stand up a local instance of it.

## Exercise 2 — HPAs

1. Now that we have a local instance running, run the included load generator script which is supposed to simulate a flash sale. This can be done with either the Taskfile or with a raw [`k6`](https://grafana.com/docs/k6/latest/examples/get-started-with-k6/test-for-performance/) command.

2. Now observe how the effect this has. The Taskfile has a simple observabity solution included, in a real enterprise system observability is generally handled with an exporter and a dashboard like the Prometheus + Grafana stack.

3. What can we observe when the load generator runs? Can the single pod keep up with the sudden influx in orders?

4. Try fixing the issue with an HPA that scales based on CPU load. Does this work?

5. What is the fundamental limitation of using CPU as a scaling signal for this workload?

## Exercise 3 — KEDA

KEDA can scale the order-processor based on how many unprocessed orders are sitting in the queue — before CPU pressure ever materializes.

Before installing KEDA, you must remove the HPA you created in Exercise 3. Only one autoscaler can manage a given Deployment at a time; leaving both in place will cause a conflict.

1. Delete your HPA and confirm it is gone before continuing.

2. Install KEDA to your cluster.

3. Verify that the KEDA controller pods are `Running` in the `keda` namespace before continuing.

## Exercise 5 — Create a ScaledObject

A `ScaledObject` is the KEDA resource that connects a scaling trigger to a Kubernetes workload.

Using the [KEDA documentation](https://keda.sh/docs/), write a `ScaledObject` that:

- Targets the `order-processor` Deployment
- Uses the appropriate scaler for a Redis list queue
- Sets a minimum and maximum replica count appropriate for this workload
- Uses a queue depth threshold that reflects how many messages a single replica can realistically keep up with

Apply your `ScaledObject` and run `kubectl get scaledobject -n keda-demo`. The `READY` column should show `True` before you proceed.

Try placing your cluster under load again. How does the scaled object compare to the HPA?

### Deliverables

- Why did the native HPA fail to protect the system under a sudden surge, even after it scaled out?
- In the producer/consumer pattern, what are the tradeoffs of scaling the consumer based on queue depth vs. scaling it based on CPU utilization?

[project]
name = "simple-system-frontend"
version = "0.1.0"
description = "Frontend web UI for the simple task list system (Chapter 11.3 example)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "Flask>=3.0,<4.0",
    "requests>=2.31,<3.0",
]

[project]
name = "simple-system-backend"
version = "0.1.0"
description = "Backend API for the simple task list system (Chapter 11.3 example)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "Flask>=3.0,<4.0",
]
